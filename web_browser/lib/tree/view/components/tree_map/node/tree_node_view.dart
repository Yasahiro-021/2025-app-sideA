import 'package:flutter/widgets.dart';

class TreeNodeView extends StatelessWidget {
  const TreeNodeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Positioned(
      top: 0,
      left: 0,
      child: Container());
  }
}
