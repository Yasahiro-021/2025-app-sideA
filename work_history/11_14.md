# 11月 14日の作業記録

## 今日の予定

昨日作成したアーキテクチャに基づいた実装

### Model層の実装

- `NodeWithPath`クラスのFreezed化
  - `parent`フィールドの扱いを検討(循環参照の回避)
  - `children` getterの実装方法を決定
- `NodePath`クラスの拡張
  - `depth` getterの実装
  - `isRoot()` メソッドの実装
  - `getParentPath()` メソッドの実装
- `NodeChildren`クラスの拡張
  - `addChild()` メソッドの実装
  - `removeChild()` メソッドの実装
  - イミュータブル性の保証

### ViewModel層の実装

- `NodeChildrenNotifier` (Family)の新規作成
  - `NodePath`をキーとしたFamily Notifierとして実装
  - `addChild()`, `removeChild()`, `updateChild()` メソッドの実装
- `CurrentNodeNotifier`の拡張
  - `navigateToParent()` メソッドの追加
  - `navigateToChild(NodePath)` メソッドの追加
  - TODOコメントの解消: ROOTノードを`RootNodeNotifier`から取得

### テストの作成

- Modelクラスのユニットテスト
- Notifierの動作確認テスト

## 完了した作業とコミット

- `NodeWithPath`クラスのFreezed化
- childrenは後回し
